# DockerCOPYINTO

A workflow designed to copy source files into a running container.

Source files are injected into the container

Changes are applied via SSH inside the container

Useful for controlled environments where host-to-container sync is not desired