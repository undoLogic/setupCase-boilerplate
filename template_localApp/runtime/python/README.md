# Python Runtime – localApp

This folder contains the Python runtime implementation for the
`template_localApp` SetupCase template.

- Entry point: `app/main.py`
- Managed with Poetry
- No build artifacts committed
- No virtual environments committed

This is a minimal boilerplate intended to be copied and ext*__*