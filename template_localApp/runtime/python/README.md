# LocalApp – Python Runtime

This folder contains the Python runtime implementation for the SetupCase
`localApp` template.

- Managed with Poetry
- No build artifacts committed
- No virtual environments committed
