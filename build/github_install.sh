# this will allow to install directly into GitHub

# Create a new GitHub repository

# local computer - checkout this new repo to your computer with phpStorm

# download the setupCase libraries
svn export --force https://github.com/undoLogic/setupCase-boilerplate/trunk .

# use launch to upload to a test server, where we will build our files
